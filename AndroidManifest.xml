<?xml version="1.0" encoding="UTF-8"?>
<manifest android:versionCode="1" android:versionName="1.0"
    package="com.androidtitlan.endeavorsubasta" xmlns:android="http://schemas.android.com/apk/res/android">
    <uses-sdk android:minSdkVersion="12"/>
    <application android:icon="@drawable/ic_launcher"
        android:label="@string/app_name"
        android:theme="@style/Theme.IOSched">
        <activity android:name=".SplashActivity"
            android:label="@string/app_name"
            android:theme="@style/Theme.IOSched.Home">
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>
                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>
        <activity android:label="@string/app_name"
            android:name=".EndeavorSubastaActivity" 
            android:theme="@style/Theme.IOSched.Home"/>
        <activity android:label="Producto n" 
            	  android:name=".ProductActivity"
            	  android:theme="@style/Theme.IOSched.Home"/>
        <activity android:label="Inicie Sesión o Regístrese"
            	android:name=".BiddingDialog" 
            	android:theme="@android:style/Theme.Dialog"/>
    </application>
</manifest>
